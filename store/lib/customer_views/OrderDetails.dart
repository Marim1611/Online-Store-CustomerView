import 'package:flutter/material.dart';
import 'calss_order.dart';

class MyOrderDetails extends StatelessWidget {

  final OrderM order;
  final int CUSTID;
  const MyOrderDetails({Key key, this.order, this.CUSTID}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
